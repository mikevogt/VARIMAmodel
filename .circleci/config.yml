version: 2
jobs:
  working_directory: ~/VARIMAmodel
  docker:
    - image: circleci/python:3.6.4
  steps:
    - checkout
    - run:
      command: |
            sudo pip install pipenv
            pipenv install
